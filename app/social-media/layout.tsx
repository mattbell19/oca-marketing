import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Social Media Strategy Course Bundle | Online Courses Australia',
  description: 'Master social media strategy, marketing, and campaign management with Online Courses Australia.'
}

export default function SocialMediaLayout({ children }: { children: React.ReactNode }) {
  return children
}
